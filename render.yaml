services:
  - type: web
    name: xledger
    runtime: python
    buildCommand: "apt-get install -y wkhtmltopdf fonts-noto fonts-noto-cjk && pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
